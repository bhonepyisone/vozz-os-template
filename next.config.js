/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optional: Add configuration for images from external domains if needed later
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;